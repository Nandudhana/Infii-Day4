def check_palindrome(word):

    #Remove pass and write your logic here
    rev= word[::-1]
    if word==rev:
      return True 
    else:
      return False  

status=check_palindrome("mal")
if(status):
    print("word is palindrome")
else:
    print("word is not palindrome")
